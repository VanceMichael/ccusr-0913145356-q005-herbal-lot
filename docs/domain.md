# 草本饮品冲泡放行引擎领域约定

    SQLite 只缓存有效限制，所有结论以 SQLite 中的检测记录为准。

    ## 核心对象

    - 原料批次：供应来源与检测结论
- 冲泡限制：温度、时长和杯量范围
- 冲突规则：不能同时使用的成分组合
- 制作凭证：当次请求和命中依据

    ## 业务边界

    - 撤回批次不得生成新凭证
- 旧凭证保留当时结论
- 缓存不得延长限制的有效期

    所有时间字段使用带时区的 ISO 8601 表示；公开标识与数据库自增键分离。接口返回的原因码必须稳定，便于调用方记录和重试。
